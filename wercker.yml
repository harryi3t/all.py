box: python
build:
  steps:
    - script:
        name: "init"
        code: |-
          [ -s requirements.txt ] && pip install -r requirements.txt
          python setup.py install
    - script:
        name: "tests"
        code: |-
          bash Tests/run.sh