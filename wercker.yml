box: python
build:
  steps:
    - script:
        name: "init"
        code: |-
          [ -s requirements.txt ] && pip install -r requirements.txt
    - script:
        name: "tests"
        code: |-
          Tests/run.sh